#include <stdio.h>
#include <unistd.h>

main ( )	
{	
    // execl تحتاج المسار الكامل للبرنامج (/bin/ls)
    execl ("/bin/ls",	/* البرنامج المراد تشغيله - المسار الكامل */
    "ls",	            /* اسم البرنامج المرسل لـ argv[0] */
    "-l",	            /* المعامل الأول (قائمة مفصلة) */
    "-a",	            /* المعامل الثاني (إظهار الكل) */
    NULL) ;	            /* يجب إنهاء القائمة بـ NULL */

    // هذا السطر لن يطبع إلا إذا فشل الـ EXEC
    printf ("EXEC Failed\n") ;
}
